Set {{ id }}: {{ name }}.
{%- if themeLine and year %}
  {%- if retiredYear %} A {{ year }} release from the {{ themeLine }}, retired in {{ retiredYear }}.
  {%- else %} A {{ year }} release from the {{ themeLine }}.
  {%- endif -%}
{%- endif -%}
{%- if isGwp %}
  {%- if gwpTargetName %} Released as a gift with purchase alongside {{ gwpTargetName }}.
  {%- else %} Released as a gift with purchase.
  {%- endif -%}
{%- endif -%}
{%- if pieces %} Features {{ pieces | format_number }} pieces.{% endif -%}
{%- if buildSpanText and buildDuration %} Built over {{ buildSpanText }} for a total of {{ buildDuration }}.
{%- elsif buildDuration %} Took {{ buildDuration }} to build.
{%- endif -%}
{%- if collectionFact %} {{ collectionFact }}{% endif -%}
{%- if primaryRatingFact %} {{ primaryRatingFact }}{% endif -%}
{%- if funFacts %} {{ funFacts }}{% endif -%}

