Lego set {{ id }}: {{ name }}.
{%- if theme and year %} A {{ year }} release from the {{ theme }} line.{% endif -%}
{%- if pieces %} Features {{ pieces | format_number }} pieces.{% endif -%}
{%- if buildDuration %} Took {{ buildDuration }} to build.{% endif -%}
{%- if collectionFact %} {{ collectionFact }}{% endif -%}
{%- if funFacts %} {{ funFacts }}{% endif -%}

