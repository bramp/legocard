Set {{ id }}: {{ name }}.

{%- if themeLine and year %} A {{ year }} release from the {{ themeLine }}{% if retiredYear %}, retired in {{ retiredYear }}{% endif %}.{% endif -%}

{%- if isGwp %}
  {%- if gwpTargetName %} Released as a gift with purchase alongside {{ gwpTargetName }}.
  {%- else %} Released as a gift with purchase.
  {%- endif -%}
{%- endif -%}

{%- if pieces and buildSpanText and buildDuration %} Built over {{ buildSpanText }}, this {{ pieces | format_number }}-piece model took a total of {{ buildDuration }} to assemble.
{%- elsif pieces and buildDuration %} This {{ pieces | format_number }}-piece model took {{ buildDuration }} to assemble.
{%- elsif pieces %} Features {{ pieces | format_number }} pieces.
{%- elsif buildSpanText and buildDuration %} Built over {{ buildSpanText }} for a total of {{ buildDuration }}.
{%- elsif buildDuration %} Took {{ buildDuration }} to build.
{%- endif -%}

{%- assign factsCount = 0 -%}
{%- if isLargest %}
  {%- assign factsCount = factsCount | plus: 1 %} It has the most pieces in the collection.
{%- elsif piecesRank >= 2 and piecesRank <= 5 %}
  {%- assign factsCount = factsCount | plus: 1 %} It is the {{ piecesRankOrdinal }} largest set in the collection.
{%- elsif isSmallest %}
  {%- assign factsCount = factsCount | plus: 1 %} It is the smallest set in the collection.
{%- elsif isThemeLargest %}
  {%- assign factsCount = factsCount | plus: 1 %} It is the largest {{ themeName }} set in the collection.
{%- elsif isTheme2ndLargest %}
  {%- assign factsCount = factsCount | plus: 1 %} It is the 2nd largest {{ themeName }} set in the collection.
{%- endif -%}

{%- if isLongestBuild %}
  {%- assign factsCount = factsCount | plus: 1 %} It is the longest build in the collection.
{%- elsif buildTimeRank >= 2 and buildTimeRank <= 5 and factsCount < 2 %}
  {%- assign factsCount = factsCount | plus: 1 %} It is the {{ buildTimeRankOrdinal }} longest build in the collection.
{%- elsif isFastestBuild %}
  {%- assign factsCount = factsCount | plus: 1 %} It is the fastest build in the collection.
{%- endif -%}

{%- if factsCount == 0 -%}
  {%- if isOldest %} It is tied for the oldest set in the collection.
  {%- elsif isNewest %} It is one of the newest additions to the collection.
  {%- endif -%}
{%- endif -%}

{%- if ratingBuild == 5 and ratingLooks == 5 %} It is rated a perfect five out of five for both build experience and display looks.
{%- elsif ratingBuild == 5 %} It is rated a top five out of five for building fun.
{%- elsif ratingLooks == 5 %} It is rated a top five out of five for display looks.
{%- elsif isTopBuild %} It is our highest-rated build experience in the collection.
{%- elsif isTopLooks %} It is our highest-rated display piece for looks.
{%- elsif ratingBuild >= 4.5 %} It is rated as one of our most fun builds in the collection.
{%- elsif ratingLooks >= 4.5 %} It is rated as one of our best-looking display models in the collection.
{%- endif -%}

{%- if funFacts %} {{ funFacts }}{% endif -%}

